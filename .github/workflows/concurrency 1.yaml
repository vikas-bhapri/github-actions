name: Concurrency 1
on:
    workflow_dispatch:
        inputs:
            environment:
                description: "Environment"
                type: environment
                required: true
    
concurrency: concurrency-1
jobs:
    sleep_job:
        runs-on: ubuntu_latest
        steps:
            - run: sleep 30
